/**
 * 개인정보 안내 문구.
 *
 * 이용자가 대부분 미성년자라 수집 항목을 최소로 잡았다 — **휴대폰 번호 하나만**.
 * (설계서에는 이름·학교도 있었지만, 참가자가 전원 같은 학교라 학교는 의미가 없고
 *  이름은 사진 전달에 쓰이지 않는다. 굳이 받으면 지켜야 할 것만 늘어난다.)
 *
 * ⚠️ 지금 구조에서는 번호를 안 받을 수가 없다.
 *    사진이 노트북에 닿는 길이 업로드뿐인데 서버가 번호를 파일명으로 쓴다.
 *    번호가 없으면 저장이 안 되고, 저장이 안 되면 인쇄할 파일도 없다.
 *    서버가 추측 불가능한 파일명을 쓰게 되면 번호를 선택 사항으로 되돌릴 수 있다.
 *
 * ⚠️ 보관 기간은 학교와 확정해야 한다. 지금은 "행사 후 N일" 로 적어두었다.
 */

export const PHOTO_KEEP_DAYS = 7;

export const CONSENT_TEXT =
  `휴대폰 번호 수집에 동의합니다. ` +
  `번호와 사진은 행사 종료 후 ${PHOTO_KEEP_DAYS}일 안에 모두 삭제합니다.`;
