# 기본은 Git 이 알아서 정규화한다
* text=auto

# Windows 스크립트는 CRLF 로 내려가야 한다.
# cmd.exe 는 LF 만 있는 배치 파일에서 label 과 goto 를 잘못 읽는 경우가 있다.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 빌드 결과물은 diff 를 접어둔다 (client/ 는 client-src/ 를 빌드한 것)
client/_next/** linguist-generated=true
