# 분경5컷 기획서

작성일: 2026-08-13

## 1. 프로젝트 개요

아이패드에서 웹으로 접속해 사진 8장을 촬영하고, 그중 5장을 사용자가 직접 선택해 다섯 컷 프레임으로 합성한다. 완성된 결과물은 노트북에 저장한다.

분당경영고등학교 학과 홍보 포토부스이며, 화면과 인화물에 쓰는 이름은 **분경5컷**이다.

- **촬영·선택·합성**: 아이패드(클라이언트)에서 전부 처리
- **저장**: 노트북(서버)이 완성 이미지를 받아 로컬 디스크에 저장
- **네트워크**: 노트북이 직접 모바일 핫스팟을 켜서 그 안에서만 통신
- **배포**: 클라우드 배포 없음. 노트북 로컬 IP 기반으로만 서비스

## 2. 기기 역할

### 아이패드 — 클라이언트

- Safari 웹브라우저로 접속
- 카메라로 8장 촬영
- 촬영본 중 5장 선택
- 프레임 선택 (두 가지 중 하나)
- `<canvas>`로 네컷 레이아웃 합성
- 전화번호 입력
- 완성 이미지와 전화번호를 서버로 전송

### 노트북 — 서버 + 핫스팟 호스트

- 모바일 핫스팟을 직접 켜서 자체 네트워크 생성 (아이패드는 이 핫스팟에 접속)
- 클라이언트 정적 파일(HTML/JS) 서빙
- `/upload` 엔드포인트로 완성 이미지 수신
- 받은 이미지를 로컬 `output/` 폴더에 파일로 저장

한 프로세스가 정적 파일 서빙과 업로드 처리를 동시에 담당한다.

## 3. 데이터 흐름

```
[아이패드]                              [노트북]
  (노트북 핫스팟에 접속)
  카메라로 8장 촬영
  5장 선택
  프레임 선택
  canvas로 합성 (사진 위에 프레임을 덮는다)
        │
        │ ① GET /              (최초 접속 시 client 파일 응답)
        │◀─────────────────────
        │
        │ ② POST /upload        (완성 이미지, HTTPS)
        │─────────────────────▶
                                    │
                                    ▼
                              output/ 폴더에 파일로 저장
```

## 4. 네트워크 구성

```
[외부망: 장소 Wi-Fi 또는 테더링]        [노트북]              [핫스팟망: 192.168.137.0/24]
  (인터넷 연결용, 매번 IP 다름)      업스트림 어댑터                  │
                                        │                       핫스팟 어댑터
                                        │                    IP: 192.168.137.1 (고정)
                                        │                            │
                                    서버 프로세스 실행 ◀──────────────┘
                                                                     │
                                                              아이패드 192.168.137.x
```

- 노트북 핫스팟 IP: `192.168.137.1` 고정
- 필요 시 수동 고정 절차:

```
1. 핫스팟 켜기
2. Win + R > ncpa.cpl
3. 핫스팟 가상 어댑터(로컬 영역 연결* 2 등) 우클릭 > 속성
4. IPv4 속성 > 다음 IP 주소 사용
   IP 주소: 192.168.137.1 / 서브넷 마스크: 255.255.255.0
```

## 5. 배포 및 HTTPS 처리

배포: 노트북이 핫스팟 IP(`192.168.137.1`)에서 client 정적 파일과 업로드 API를 함께 서빙. 클라우드 배포 없음.

### 5.1 인증서 발급

자체 서명 CA를 만들고 그 CA로 서버 인증서를 발급한다. npm `mkcert` 패키지를 사용하며, 발급 스크립트는 `server/generate-cert.js`.

```
cd server
node generate-cert.js
```

생성 파일:

| 경로 | 용도 |
|---|---|
| `server/certs/server.pem` | 서버 인증서 |
| `server/certs/server-key.pem` | 서버 키 |
| `server/certs/ca/rootCA.pem` | CA 인증서 — 접속 기기에 설치할 파일 |
| `server/certs/ca/rootCA-key.pem` | CA 키 (유출 주의) |

서버 인증서에 포함된 주소: `192.168.137.1`, `192.168.138.1`, `localhost`, `127.0.0.1`
만료: 2028-11-15

CA가 이미 있으면 스크립트가 기존 CA를 재사용한다. 기기에 한 번 설치한 CA를 계속 쓰기 위함이며, CA를 새로 만들면 모든 기기에서 재설치가 필요하다.

### 5.2 접속 기기 인증서 설치

Safari의 카메라 API(`getUserMedia`)는 신뢰된 HTTPS에서만 동작한다. 자체 서명 CA는 iOS가 기본으로 신뢰하지 않으므로, 접속할 기기마다 CA를 설치하고 신뢰 설정을 켜야 한다.

```
1. server/certs/ca/rootCA.pem 을 기기로 전달 (메일 / 클라우드 / AirDrop)
2. 기기에서 파일을 열면 "프로파일이 다운로드됨" 알림이 뜬다
3. 설정 > 일반 > VPN 및 기기 관리 > 해당 프로파일 > 설치
4. 설정 > 일반 > 정보 > 인증서 신뢰 설정 > LifeFivePhoto Local CA 켜기
```

4번을 빼먹으면 프로파일을 설치해도 Safari가 인증서를 신뢰하지 않는다.

### 5.3 제약: 기기마다 인증서 설치 필요

이 방식은 접속하는 기기마다 위 설치 절차를 최초 1회 거쳐야 한다. 기기를 초기화하거나 새 기기를 추가하면 다시 설치해야 한다.

설치를 건너뛰면 Safari가 "이 연결은 비공개 연결이 아닙니다" 경고를 띄운다. 경고를 무시하고 접속할 수는 있으나, iOS는 인증서 경고를 통과한 페이지를 보안 컨텍스트로 취급하지 않으므로 카메라 API가 계속 차단된다. 즉 촬영 기능이 동작하지 않는다.

**공인 인증서를 쓸 수 없는 이유**

공인 CA는 도메인 소유권 증명을 요구한다. `192.168.137.1`은 사설 IP라 소유 개념이 없어 CA/Browser Forum 규정상 발급이 금지되며, 도메인을 연결하더라도 공인 CA가 검증을 위해 해당 주소에 접속할 수 없다.

**제약이 문제가 되는 경우의 대안**

사용 기기가 고정되어 있으면 최초 1회 설치로 끝나므로 현재 방식을 유지한다. 불특정 기기가 접속하는 구조로 바뀌면 아래 방식으로 전환한다.

| 항목 | 현재 (자체 서명 CA) | 대안 (공인 인증서, DNS-01) |
|---|---|---|
| 비용 | 없음 | 도메인 비용 (연 1~2만원) |
| 기기 준비 | 기기마다 최초 1회 설치 | 불필요 |
| 갱신 | 2028-11-15까지 불필요 | 90일마다 갱신 (자동화 필요) |
| 인터넷 | 불필요 | 발급·갱신 시 필요 |

대안 방식은 도메인을 구입해 A 레코드를 `192.168.137.1`로 지정하고, Let's Encrypt DNS-01 챌린지로 인증서를 발급받는다. DNS TXT 레코드로 소유권을 증명하므로 서버가 인터넷에서 도달 가능할 필요가 없다.

**터널 방식 (검토 후 미채택)**

Cloudflare Tunnel / ngrok으로 노트북 서버를 공인 도메인에 노출하면 인증서 설치가 불필요해진다. 서버 코드 변경 없이 별도 프로세스만 띄우면 된다.

```
cloudflared tunnel --url https://localhost:3000
```

채택하지 않은 이유는 사용 전제가 다르기 때문이다. 터널을 쓰면 접속 기기가 노트북 핫스팟에 붙을 필요가 없어지는 대신, 노트북과 접속 기기 양쪽에 상시 인터넷이 필요하다. 사진 데이터도 인터넷을 왕복하므로 업로드 속도가 노트북의 상단 회선에 묶이고, 장소 인터넷 품질에 운영 안정성이 종속된다. 핫스팟 구조를 택한 목적과 상충한다.

사용 기기가 고정된 아이패드 1~2대이므로 인증서 최초 1회 설치가 더 유리하다고 판단했다. 불특정 기기(손님 개인 휴대폰 등)가 접속하는 구조로 바뀌면 터널 또는 공인 인증서 방식을 재검토한다.

## 6. API 명세서

베이스 주소: `https://192.168.137.1:3000`

### GET /

클라이언트 진입 페이지(`index.html`) 응답.

| 항목 | 내용 |
|---|---|
| 요청 | 없음 |
| 응답 | `200 OK`, `Content-Type: text/html` |

### GET /camera.js, /compose.js 등 정적 리소스

`client/` 폴더 내 정적 파일을 그대로 서빙.

| 항목 | 내용 |
|---|---|
| 요청 | 없음 |
| 응답 | `200 OK`, `Content-Type: application/javascript` (또는 해당 MIME 타입), 없는 파일 요청 시 `404 Not Found` |

### POST /upload

완성된 네컷 합성 이미지 1장을 업로드한다.

**요청**

| 항목 | 값 |
|---|---|
| Content-Type | `multipart/form-data` |
| 필드명 | `photo` |
| 필드 타입 | File (Blob) |
| 이미지 형식 | `image/png` (`canvas.toBlob()` 결과) |
| 필드명 | `phone` |
| 필드 타입 | 문자열 (전화번호) |

```
POST /upload HTTP/1.1
Content-Type: multipart/form-data; boundary=...

--boundary
Content-Disposition: form-data; name="phone"

01012345678
--boundary
Content-Disposition: form-data; name="photo"; filename="capture.png"
Content-Type: image/png

<binary data>
--boundary--
```

**성공 응답 — 200 OK**

```json
{
  "success": true,
  "filename": "2026-08-13/01012345678.png"
}
```

**실패 응답 — 400 Bad Request** (이미지 파일 또는 전화번호 누락)

```json
{
  "success": false,
  "error": "전화번호가 없습니다."
}
```

**실패 응답 — 500 Internal Server Error**

```json
{
  "success": false,
  "error": "파일 저장에 실패했습니다."
}
```

### GET /health

| 항목 | 내용 |
|---|---|
| 요청 | 없음 |
| 응답 | `200 OK`, `{ "status": "ok" }` |

### 저장 파일명 규칙

```
바탕화면/output/{날짜}/{전화번호}.png   예) 바탕화면/output/2026-08-13/01012345678.png
```

날짜 폴더(YYYY-MM-DD)는 업로드 시점 기준으로 자동 생성된다.

저장 위치를 바탕화면으로 둔 이유는 행사 중 운영자가 사진을 찾아 인쇄해야 하기 때문이다. 경로는 홈 디렉터리를 기준으로 찾으며, OneDrive 백업으로 바탕화면이 옮겨간 경우도 함께 살펴본다. `OUTPUT_DIR` 환경변수로 덮어쓸 수 있다.

## 7. 방화벽 설정 (완료)

| 항목 | 값 |
|---|---|
| 이름 | 인생오컷_서버 |
| 방향 | Inbound |
| 동작 | Allow |
| 상태 | Enabled |
| 프로필 | Any |
| 포트 | 3000 / TCP |

## 8. 로컬 디렉토리 구조

```
LifeFivePhoto/
├── CLAUDE.md                   실행 전 설명 규칙 (작업 진행 시 준수)
├── README.md                   실행 방법과 검증 상태
├── 기획서.md                    본 문서
├── 부스-시작.bat                핫스팟 켜기 + 서버 실행
├── 부스-종료.bat                서버 멈추기 + 핫스팟 끄기
├── scripts/
│   ├── start-booth.ps1         사전 점검 후 핫스팟과 서버를 띄운다
│   ├── stop-booth.ps1          3000번 포트의 서버를 멈추고 핫스팟을 끈다
│   └── hotspot.ps1             핫스팟 on/off/status
├── server/                     노트북에서 실행
│   ├── server.js               정적 파일 서빙 + /upload, /health 처리
│   ├── generate-cert.js        CA 및 서버 인증서 생성
│   ├── test-api.js             API 명세 대조 테스트
│   ├── test-e2e.js             부스 흐름 통합 테스트
│   ├── package.json
│   └── certs/                  저장소에서 제외 (각자 생성)
│       ├── server.pem          서버 인증서
│       ├── server-key.pem      서버 키
│       └── ca/
│           ├── rootCA.pem      접속 기기에 설치할 CA 인증서
│           └── rootCA-key.pem  CA 키 (유출 주의)
├── client/                     빌드 결과물 — 서버가 이 폴더를 서빙한다
│   ├── index.html
│   ├── 404.html
│   ├── manifest.webmanifest
│   ├── brand/                  학교·학과 이미지
│   ├── debug/                  운영자용 점검 화면
│   └── _next/                  Next.js 정적 자산
└── client-src/                 클라이언트 소스 (Next.js, pnpm)
    ├── src/
    │   ├── app/                화면 진입점
    │   ├── components/booth/   단계별 화면
    │   ├── hooks/              카메라, 화면 꺼짐 방지
    │   └── lib/                합성, 업로드, 프레임 규격과 목록
    └── public/
        └── frames/             프레임 그림 (사진 자리가 투명한 PNG)

바탕화면/
└── output/                     완성본 저장 위치 (프로젝트 폴더 바깥)
    └── 2026-08-13/             날짜별 폴더 (YYYY-MM-DD)
        └── 01012345678.png     전화번호 기준 파일명
```

행사 당일에는 `부스-시작.bat` 과 `부스-종료.bat` 두 개로 운영한다. 시작 쪽은 Node.js 설치 여부, 의존성, 인증서, 3000번 포트 점유를 먼저 확인하고 빠진 것은 알아서 채운 뒤 핫스팟과 서버를 띄운다. 종료 쪽은 3000번 포트를 쥐고 있는 프로세스만 골라 멈추고 핫스팟을 끈다.

명령으로 직접 실행하려면:

```
cd server
npm install
node generate-cert.js   # 최초 1회
npm start
```

클라이언트를 고쳤다면 빌드해서 `client/` 로 옮겨야 반영된다:

```
cd client-src
pnpm install
pnpm build
robocopy out ..\client /MIR
```

## 9. 진행 상황

**완료**

- [x] Windows 방화벽 인바운드 규칙 등록 (7장 참고)
- [x] `server/server.js` 작성 — 정적 파일 서빙, `/upload`, `/health` 라우트, `output/{날짜}/{전화번호}.png` 저장
- [x] 인증서 발급 — `server/generate-cert.js`, `server/certs/` 생성 완료
- [x] 노트북 모바일 핫스팟 동작 확인 — `192.168.137.1` 부여됨 (수동 고정 불필요)
- [x] 서버 HTTPS 기동 및 API 명세 전 항목 동작 검증 (16개 항목, `npm test`)
- [x] GitHub 저장소 등록 — https://github.com/picky232/LifeFivePhoto (Public, 키·결과물 제외)
- [x] 클라이언트 구현 병합 — 촬영·선택·합성·번호 입력·업로드 (Next.js 정적 빌드)
- [x] 부스 흐름 통합 테스트 — 브라우저에서 촬영부터 저장까지 9단계 통과 (`npm run test:e2e`)
- [x] 실기기(iOS) 확인 — CA 설치 후 iPhone Safari 에서 촬영·업로드 동작. 인증서가 신뢰되지 않으면 `getUserMedia` 가 막혀 촬영 자체가 되지 않으므로, 업로드된 파일이 남았다는 것은 인증서 설치가 유효했다는 뜻이다
- [x] 저장 위치를 바탕화면 `output/` 으로 변경 (운영 중 인쇄 편의)
- [x] 프레임 선택 단계 추가 — 사진을 깔고 프레임 그림을 덮는 방식으로 바꿈 (12장)

**남은 작업**

- [ ] 아이패드 화면 크기에서 레이아웃 확인 (지금까지 확인은 iPhone 과 노트북 브라우저 기준)
- [ ] 다른 장소로 이동해 핫스팟 재구성 후 전체 플로우 재검증
- [ ] 여러 명이 연달아 사용할 때의 안정성 확인
- [ ] 인쇄 절차 확정 (저장된 파일을 어떤 방식으로 뽑을지)
- [ ] 실물 프레임 두 종 받기 — PNG 를 받으면 투명 영역 좌표를 재서 `SLOTS` 에 반영
- [ ] 인쇄 용지 확정 (4×6 vs 4×5) — 프레임 외주 전에 정해야 한다

## 10. 구현 세부 결정

기획 확정 이후 구현 과정에서 정한 사항.

| 항목 | 결정 |
|---|---|
| 전화번호 검증 | 숫자만 추출 후 9~11자리만 허용. 하이픈 입력은 자동 제거. 이 값이 파일명이 되므로 검증 없이는 상위 경로 이동 등 의도하지 않은 위치에 파일이 쓰일 수 있어 필수 |
| 파일명 중복 | 같은 날 같은 번호로 재촬영 시 덮어쓰지 않고 `01012345678_2.png` 형태로 저장 |
| 업로드 용량 제한 | 20MB, 파일 1개. 실제 합성본은 1200×1800 PNG 로 1~7MB 수준 |
| 인증서 미존재 시 | HTTP로 기동하고 경고 출력. 이 경우 카메라 API는 동작하지 않음 |
| 저장 위치 | 바탕화면 `output/`. 경로를 박지 않고 홈 디렉터리 기준으로 찾으며 OneDrive 로 옮겨진 바탕화면도 살펴본다 |
| 404 응답 | 클라이언트 빌드의 `404.html` 로 응답한다. 그냥 두면 Express 기본 영어 오류 페이지가 부스 화면에 노출된다 |
| 환경변수 | `PORT`, `OUTPUT_DIR`, `CERT_DIR` 로 덮어쓸 수 있다. 테스트가 실제 저장 폴더와 운영 포트를 건드리지 않기 위한 것이다 |
| 클라이언트 배포 | `client/` 는 `client-src/` 빌드 결과물이며 저장소에 커밋한다. 행사 노트북에서 빌드 없이 바로 서빙하기 위해서다 |

## 11. 화면 문구 결정

원본 사진을 문자로 보내는 기능은 현재 범위에 없다. 없는 기능을 약속하는 문구를 모두 걷어냈다.

| 위치 | 변경 |
|---|---|
| 미리보기 화면 버튼 | `이 사진으로 인화하기` → `선택 완료` |
| 번호 입력 화면 설명 | "원본 사진도 이 번호로 보내드립니다" 삭제 |
| 번호 입력 화면 버튼 | `인화하고 사진 받기` → `확인` |
| 동의 문구 | "사진을 보내드리기 위해 휴대폰 번호를 받습니다" → "휴대폰 번호 수집에 동의합니다" (7일 내 삭제 문구는 유지) |
| 인쇄 요청 화면 | "원본 사진은 잠시 뒤 (번호) 으로 보내드립니다" 삭제 |
| 안내 화면 | "원본 사진은 휴대폰으로도 보내드려요" 삭제 |

번호는 계속 받는다. 서버가 번호를 파일명으로 쓰기 때문에 번호가 없으면 저장도 인쇄도 되지 않는다.

## 12. 프레임

인화물은 **사진을 먼저 깔고 그 위에 프레임 그림을 덮어** 만든다. 프레임은 사진이 빠진 뼈대이고, 사진 자리는 투명하게 뚫려 있다. 테두리, 장식, 학과 이름표, 로고가 모두 프레임 그림 안에 들어 있다.

순서가 중요하다. 프레임에는 사진 자리 밖으로 걸치는 장식이 있을 수 있고, 그런 장식은 사진 위에 얹혀야 한다. 뒤에 깔면 사진에 가려 보이지 않는다.

예전에는 코드가 배경과 학과 이름표와 로고를 직접 그렸다. 지금은 그 일이 모두 프레임 그림으로 넘어갔고, 코드는 사진을 칸에 채우고 프레임을 덮는 일만 한다.

### 규격

| 항목 | 값 |
|---|---|
| 캔버스 | 1200 × 1800 px, 300dpi, sRGB |
| 형식 | PNG-24 (알파 채널 포함) |
| 실치수 | 101.6 × 152.4 mm (4 × 6 인치) |

사진 칸 다섯 자리. `client-src/src/lib/frame.ts` 의 `SLOTS` 가 기준이다.

| 칸 | x | y | 크기 |
|---|---|---|---|
| 1 | 42 | 42 | 549 × 560 |
| 2 | 42 | 620 | 549 × 560 |
| 3 | 42 | 1198 | 549 × 560 |
| 4 | 609 | 42 | 549 × 560 |
| 5 | 609 | 620 | 549 × 560 |
| 로고 | 609 | 1198 | 549 × 560 |

칸 높이 560 중 아래 74px 은 학과 이름표 자리다. 이름표를 프레임에 그려 넣으면 그 부분은 불투명하게 두고 위쪽 486px 만 뚫으면 된다.

### 칸 자리는 프레임마다 같다

프레임이 여러 개여도 **사진 칸의 위치와 크기는 모두 같다.** 디자인만 다르다.

칸 비율이 프레임마다 다르면 촬영 단계가 꼬인다. 사진은 찍는 순간 컷 비율로 잘라 저장하는데, 프레임은 그 뒤에 고르기 때문이다. 정사각형으로 잘라 둔 사진을 가로로 긴 칸에 넣으면 위아래가 더 잘려 머리가 날아간다. 없는 부분은 되살릴 수 없다.

자리를 고정해두면 이 문제가 생기지 않고, 프레임을 추가할 때 좌표를 다시 재지 않아도 된다.

### 고르는 화면

이름만 늘어놓고 고르게 하면 뭐가 다른지 알 수 없다. 그래서 **고른 사진이 실제로 들어간 모습**을 프레임마다 그려서 나란히 보여준다. 거기 보이는 그림이 그대로 인화된다.

미리보기 화면은 이 단계에서 만든 그림을 그대로 받는다. 다시 만들면 같은 일을 두 번 하고 그사이 화면이 비어 보인다.

프레임 수만큼 합성이 돌기 때문에 프레임이 많아지면 느려진다. 그때는 한 칸짜리 견본으로 바꾸면 된다.

### 프레임 추가

`client-src/public/frames/` 에 PNG 를 넣고 `client-src/src/lib/frames.ts` 에 한 줄 적는다.

```ts
{ id: "spring", name: "봄", note: "한 줄 설명", image: "/frames/spring.png" }
```

### 지금 들어 있는 것

실물 디자인이 오기 전까지 흐름을 확인하려고 만든 임시 그림 두 개다. 실물이 오면 같은 자리에 파일만 바꿔 끼운다. 좌표가 다르면 `frame.ts` 의 `SLOTS` 만 고치면 화면과 인화물이 함께 따라온다.

## 13. 이름과 색

### 이름

화면과 인화물에 쓰는 이름은 **분경5컷**이다. 시작 화면 제목, 스플래시, 인화물 로고 칸, 브라우저 제목, 홈 화면에 추가했을 때 이름이 모두 이 이름을 쓴다.

인화물 로고 칸의 글자는 캔버스에 직접 그린다. 캔버스는 화면과 달리 글꼴을 기다려주지 않으므로, 찍을 글자를 미리 알려주지 않으면 기본 글꼴로 나온다. 이름을 바꿀 때 `compose.ts` 의 글꼴 미리 불러오기 힌트도 함께 고쳐야 한다.

### 색

| 쓰임 | 값 |
|---|---|
| 강조 (면) | `#81d8d0` |
| 강조 (종이 위 글자) | `#257971` |
| 바탕 | `#f2f0e9` |
| 글자·선 | `#0a0a0a` |
| 어두운 화면 | `#121212` |

강조색은 면으로 쓰고 그 위에는 검정 글자를 얹는다. 같은 색을 종이 위에 글자로 올리면 대비가 1.45:1 이라 읽히지 않으므로, 색상(174.5도)과 채도는 그대로 두고 명도만 낮춘 값을 따로 둔다. 안내 화면의 단계 이름이 여기 해당한다.

프레임 외주에 전달할 때도 이 값을 함께 준다. 그래야 화면과 인쇄물이 같은 색으로 보인다.

### 학과 다섯

배열 순서가 곧 인화물의 컷 순서다. 순서를 바꾸려면 `departments.ts` 의 배열만 고치면 화면과 인화물이 함께 따라온다.

| 순서 | 학과 | 색 |
|---|---|---|
| 1 | 회계금융과 | `#787cb6` |
| 2 | 스마트경영과 | `#1189ca` |
| 3 | 인공지능개발과 | `#0db04b` |
| 4 | 스마트호텔관광과 | `#f48020` |
| 5 | 그래픽디자인과 | `#81d8d0` |

학과 색은 어두운 화면 위에서 4.78 ~ 11.30:1 로 모두 기준(3.0:1)을 넘는다. 회계금융과는 학교 공식 남색(`#2f368f`)이 대비 1.82 로 미달이라 그 색을 밝힌 값을 쓴다.

인화물의 학과 이름표는 캔버스에 직접 그리는 글자라 넘쳐도 알아서 줄이거나 접지 않는다. 마스코트가 오른쪽에 서서 쓸 수 있는 폭이 정해져 있으므로, **그 폭에 들어갈 때까지 글자 크기를 한 단계씩 줄인다.** 다섯 자 이름은 원래 크기로 나오고 여덟 자(`스마트호텔관광과`)만 조금 작아진다. 학과 이름이 더 길어져도 같은 방식으로 들어간다.

### 만든 사람

스플래시에 학과별로 한 줄씩 올린다. 한 줄에 이름만 늘어놓으면 어느 이름이 어느 학과인지 읽히지 않는다.

```
인공지능개발과   신지호 윤지원
그래픽디자인과   김도영 조서연
```

## 14. 범위 제외 사항

- 클라우드 배포, 핫스팟 밖에서의 외부 접속
- 다중 사용자 동시 업로드 처리
- 로그인·인증
- 원본 사진 문자 전송 (11장 참고)
- 인쇄 자동화 — 저장된 파일을 운영자가 직접 뽑는다
